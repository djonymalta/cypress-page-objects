{
  "name": "cypress-page-objects",
  "version": "1.0.0",
  "description": "Testes Automatizados utilizando Cypress | Page Objects | Github Actions",
  "main": "index.js",
  "scripts": {
    "cy:run": "npx cypress run --headless --browser chrome --config defaultCommandTimeout=10000"
  },
  "author": "DJONY MALTA",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.7.0"
  }
}
